import express from "express"

const router = express.Router()

// overview of trello data models
// each user has multiple boards (no auth for now)
// each board has lists 
// each list has tiles

router.get("/:id",)